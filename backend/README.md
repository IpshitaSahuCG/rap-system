# RAP Backend - FastAPI

## Overview
FastAPI backend for the Retail Analysis Platform (RAP) system. Provides bulk API endpoints for retrieving unavailable items from multiple restaurant locations.

## Prerequisites
- Python 3.9 or higher
- pip (Python package manager)

## Installation

1. **Create a virtual environment**:
   ```bash
   python -m venv venv
   source venv/bin/activate  # On Windows: venv\Scripts\activate
